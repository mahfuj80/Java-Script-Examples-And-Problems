function add(num1, num2) {
    const sum = num1 + num2;
    return sum;
}

const result1 = add(12, 13);
const result2 = add(35, 7);
const finalResult = add(result1, result2);

/**
 * What is function?
 * A procedure;
 * A set of smaller tasks that performs a bigger task when something calls it
 * maintains a relationship with input (the raw products which is given a function to perform task) and output (the ready product)
 * 
 * 
 * What is function in JS?
 * A block of code.
 * A set of statements (subprogram/ block of code) that performs a task when it is called.
 * Maintains a relationship with input and output.
 * 
 * 
 * 
 * Different syntax
 * Functions can be executed as many times (1, 2, .......... N times) you want whereas a loop is executed a specific times when is performed ( you can't perform other task in this time).
 * You can give a specific name to a  function whereas you can not give any name to a loop.
 * 
 * 
 * 
 * 
 * Function Combination:
 * No return & No parameter
 * Has return & No parameter
 * No return & Has parameter
 * Has return & Has parameter
 * 
 * Example of No return & No parameter:
 * imagine Situation-1, 
 * You have bought some fruits from market. As it is summer, you want make fruit juice and save for future.
 * So, You are thinking, "i'll make juice, pour it in a glass and save it"
 * 
 * function prepareJuice(){
 *      //make juice using a fruit
 *      // save for future
 * }
 * prepareJuice();
 * 
 *
 */